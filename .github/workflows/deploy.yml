name: Deploy Worker to Cloudflare

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy OpenNext Cloudflare Worker
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build Core package
        run: bun run --filter core build

      - name: Build OpenNext Cloudflare Worker
        run: cd apps/web && npx @opennextjs/cloudflare build

      - name: Deploy to Cloudflare Workers
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: a1ffb720cfcefc6e5e5c587480fa218d
        run: cd apps/web && npx wrangler deploy
