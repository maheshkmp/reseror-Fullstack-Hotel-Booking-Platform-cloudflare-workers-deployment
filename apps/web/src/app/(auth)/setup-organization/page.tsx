import { AgentSetupForm } from "@/features/auth/components/agent-setup-form";
import { Suspense } from "react";

export default function AgentSetupPage() {
  return <Suspense><AgentSetupForm /></Suspense>;
}
